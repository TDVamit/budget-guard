import React from 'react';

/**
 * Retired with the glass world: a monochrome surface has nothing to sit over.
 * Kept as a no-op so screens that mount it stay untouched.
 */
export function AmbientBackground() {
  return null;
}
