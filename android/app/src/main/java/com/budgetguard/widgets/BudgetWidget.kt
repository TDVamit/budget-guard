package com.budgetguard.widgets

import com.reactnativeandroidwidget.RNWidgetProvider

/**
 * Class name doubles as the widgetName the JS widgetTaskHandler receives
 * (react-native-android-widget uses getClass().getSimpleName()), so this
 * must stay in sync with WIDGET_NAME in src/widgets/widgetTaskHandler.tsx.
 */
class BudgetWidget : RNWidgetProvider()
